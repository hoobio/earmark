<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Earmark.App.Controls.ColourSwatchPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Earmark.App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="{StaticResource SpacingMedium}" Width="280">
        <!-- Swatch grid (the bright Fluent accent palette). Built in code so the selected swatch
             can show a check. -->
        <controls:WrapPanel x:Name="SwatchHost" HorizontalSpacing="6" VerticalSpacing="6" />

        <!-- Custom colour (wheel in a flyout so it doesn't crowd the host) + optional None. -->
        <StackPanel Orientation="Horizontal" Spacing="{StaticResource SpacingSmall}">
            <Button Content="Custom…">
                <Button.Flyout>
                    <Flyout>
                        <ColorPicker x:Name="WheelPicker"
                                     ColorSpectrumShape="Ring"
                                     IsAlphaEnabled="False"
                                     IsMoreButtonVisible="False"
                                     IsColorSliderVisible="True"
                                     IsColorChannelTextInputVisible="False"
                                     IsHexInputVisible="True"
                                     ColorChanged="OnWheelColorChanged" />
                    </Flyout>
                </Button.Flyout>
            </Button>
            <Button x:Name="NoneButton"
                    Content="None"
                    Visibility="Collapsed"
                    Click="OnNoneClicked"
                    ToolTipService.ToolTip="No accent - use the plain tile" />
        </StackPanel>
    </StackPanel>
</UserControl>
