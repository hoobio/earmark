<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Earmark.App.Views.RulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Earmark.App.ViewModels"
    xmlns:models="using:Earmark.Core.Models"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,16">
            <TextBlock Text="Rules" Style="{StaticResource PageHeaderTextStyle}" />
            <Button Content="Add rule" Click="OnAddClicked" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center" />
            <Button Content="Reapply now" Command="{x:Bind ViewModel.ReapplyCommand}" VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="Match a process by regex (process name or full path) and route it to a device whose name matches another regex."
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Margin="0,0,0,16"
                   Opacity="0.75" />

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Items}"
                  SelectionMode="Single"
                  SelectedItem="{x:Bind ViewModel.Selected, Mode=TwoWay}"
                  IsItemClickEnabled="True"
                  ItemClick="OnRuleClicked">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="8" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:RoutingRule">
                    <Border Style="{StaticResource SectionCardStyle}" Margin="0,4">
                        <Grid ColumnSpacing="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ToggleSwitch IsOn="{x:Bind Enabled, Mode=TwoWay}" OffContent="" OnContent="" VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" Spacing="4">
                                <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE71B;" FontSize="12" Opacity="0.6" />
                                    <TextBlock Text="{x:Bind AppPattern}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.85" />
                                    <FontIcon Glyph="&#xE72A;" FontSize="12" Opacity="0.6" />
                                    <TextBlock Text="{x:Bind DevicePattern}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.85" />
                                </StackPanel>
                            </StackPanel>
                            <Border Grid.Column="2" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="4" Padding="8,3" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind AppMatchTarget}" Style="{StaticResource CaptionTextBlockStyle}" />
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
